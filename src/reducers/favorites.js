import * as actionTypes from "../actions/actionTypes";

const favorites = (
  favorites = {
    favoritesArr: [],
    isLoading: true,
    error: null,
  },
  action
) => {
    switch()
};

export default favorites;
